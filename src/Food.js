import { useState } from "react";

/** Food: displays list of links in the router
 * 
 * State:
 * -none
 * 
 * Props;
 * -none
 * 
 * App --> Food --> Item
 */
 function Food(props) {
  return (
    <div className="Food">
      <p>This is all food for now</p>
    </div>
  );
}

export default Food;
